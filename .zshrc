# custom keybinds
bindkey '^[[1~' beginning-of-line
bindkey '^[[4~' end-of-line

# custom added aliases
alias getkeysequence='cat -v'
alias keymapswitch='setxkbmap -layout fr -variant us'
alias sysupdate='sudo apt update && sudo apt dist-upgrade && sudo apt autoremove'
